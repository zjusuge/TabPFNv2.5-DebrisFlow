# Python dependencies
# Debris-Flow Volume Prediction — TabPFN-2.5 Benchmark
# Install: pip install -r requirements.txt
# ===========================================================================

# --- Core scientific computing ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- Machine learning ---
scikit-learn>=1.3.0              # Ridge, Lasso, ElasticNet, SVR, KNN,
                                 # RF, GBR, AdaBoost, MLP, StandardScaler
xgboost>=2.0.0                   # XGBRegressor

# --- Bayesian hyperparameter optimisation ---
optuna>=3.4.0                    # TPE sampler, inner 3-fold CV tuning

# --- Pretrained tabular foundation model ---
tabpfn>=2.0.0                    # TabPFN-2.5 (Hollmann et al., 2025)
                                 # Model weights auto-downloaded from
                                 # Hugging Face on first use;
                                 # run `huggingface-cli login` beforehand